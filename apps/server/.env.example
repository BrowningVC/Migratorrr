# ===========================================
# MIGRATORRR SERVER - Environment Variables
# ===========================================

# Database (Railway provides this automatically when you add PostgreSQL)
DATABASE_URL="postgresql://user:password@host:5432/database"

# Redis (Railway provides this automatically when you add Redis)
REDIS_URL="redis://host:6379"

# Server Configuration
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info
NODE_ENV=production

# CORS - Set to your frontend domain (Railway will give you a URL)
# Example: https://migratorrr-web-production.up.railway.app
CORS_ORIGIN=https://your-frontend-domain.com

# ===========================================
# CRITICAL - Required for sniper execution
# ===========================================

# Security - Generate with: openssl rand -hex 32
# Required for encrypting user wallet private keys
# WARNING: If you lose this key, all encrypted wallets become inaccessible!
MASTER_ENCRYPTION_KEY=

# JWT Secret - Generate with: openssl rand -hex 32
# Used for signing authentication tokens
JWT_SECRET=

# Admin Secret - Generate with: openssl rand -hex 32
# Used for admin WebSocket access
ADMIN_SECRET=

# Helius API Key - Get from https://dev.helius.xyz/
# Required for RPC access, transaction submission, and migration detection
# Recommended: Developer plan ($0) or Growth plan for higher limits
HELIUS_API_KEY=

# Platform Fee Wallet - MUST be a valid Solana wallet address you control
# This is where the 1% platform fees will be sent
# Example: FEE123456789abcdefghijklmnopqrstuvwxyz12345
PLATFORM_FEE_WALLET=

# ===========================================
# Optional Configuration
# ===========================================

# Backup RPC (recommended for redundancy)
# Example: https://api.mainnet-beta.solana.com
BACKUP_RPC_URL=

# Jito Block Engine (default is mainnet)
JITO_BLOCK_ENGINE_URL=https://mainnet.block-engine.jito.wtf

# Platform fee in basis points (100 = 1%)
PLATFORM_FEE_BPS=100

# Worker tuning (adjust based on server resources)
SNIPE_WORKER_CONCURRENCY=25
SNIPE_RATE_LIMIT=200

# ===========================================
# MIGRATORRR WEB - Environment Variables
# ===========================================
# All variables prefixed with NEXT_PUBLIC_ are exposed to the browser

# Backend API URL (your server's Railway URL)
# Example: https://migratorrr-server-production.up.railway.app
NEXT_PUBLIC_API_URL=https://your-server-domain.com

# Solana RPC for client-side reads (wallet connection, balance checks)
# Can use public RPC or your own Helius endpoint
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com

# Socket.IO URL (same as API URL since socket runs on same server)
# Example: https://migratorrr-server-production.up.railway.app
NEXT_PUBLIC_SOCKET_URL=https://your-server-domain.com
